# Secrets / env (NE JAMAIS committer)
.env
*.env
**/.env
!.env.example
!**/.env.example

# Certificats Let's Encrypt (clé privée)
traefik/letsencrypt/
**/acme.json

# Dépendances / build
node_modules/
**/node_modules/

# Données / uploads
data/
uploads/

# Divers
*.log
.DS_Store

# Exports de projets (données clients, image incluse)
*.permab.json

# Sauvegardes / copies de la base et des images de prod (données clients)
*.sql
*.tgz
prod-uploads/
